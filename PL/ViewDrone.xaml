<Window x:Class="PL.ViewDrone"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="ViewDrone" Height="440" Width="800">
    <Grid>
        <Image x:Name="BackgroundDrones" Width="800" MinWidth="1" Height="450" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="-15,-24,0,0">

            <Image.Source>
                <BitmapImage  UriSource="/Images/DroneView.png" />
            </Image.Source>
        </Image>
        <Button x:Name="BringParcel" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="Bring the parcel to customer" HorizontalAlignment="Left" Margin="552,325,0,0" VerticalAlignment="Top" Width="207" Height="20" Click="Bring_Click"  Background="{x:Null}" FontStyle="Normal" FontWeight="Normal" BorderBrush="{x:Null}">
            
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button x:Name="CollectParcel" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="Collect parcel" HorizontalAlignment="Left" Margin="557,371,0,0" VerticalAlignment="Top" Width="208" Height="20" Click="Collect_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button x:Name="UpdateButton" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="Update" HorizontalAlignment="Left" Margin="32,325,0,0" VerticalAlignment="Top" Width="208" Height="20" Click="Update_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button x:Name="SendDroneToCharge" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="Send drone to charge" HorizontalAlignment="Center" Margin="6,327,0,0" VerticalAlignment="Top" Width="208" Height="20" Click="Charge_Click" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="-0.136"/>
                    <RotateTransform/>
                    <TranslateTransform Y="-0.248"/>
                </TransformGroup>
            </Button.RenderTransform>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button x:Name="RealseDroneFromeCharge" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="Realse drone from charging" HorizontalAlignment="Center" Margin="7,371,0,0" VerticalAlignment="Top" Width="208" Height="20" Click="Realse_Click" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="-0.136"/>
                    <RotateTransform/>
                    <TranslateTransform Y="-0.248"/>
                </TransformGroup>
            </Button.RenderTransform>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button x:Name="DroneToDelevery" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="Send drone to delivery" HorizontalAlignment="Left" Margin="35,371,0,0" VerticalAlignment="Top" Width="208" Height="20" Click="Send_Click" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="-0.136"/>
                    <RotateTransform/>
                    <TranslateTransform Y="-0.248"/>
                </TransformGroup>
            </Button.RenderTransform>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button x:Name="ParcelView" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="Parcel details" HorizontalAlignment="Left" Margin="51,212,0,0" VerticalAlignment="Top" Width="93" Height="70" Click="PacelDetaitls_Click" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="-0.136"/>
                    <RotateTransform/>
                    <TranslateTransform Y="-0.248"/>
                </TransformGroup>
            </Button.RenderTransform>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Button x:Name="Cancel" Foreground="White" FontFamily="Fonts/#Alfa Slab One"  Content="" HorizontalAlignment="Left" Margin="743,13,0,0" VerticalAlignment="Top" Width="29" Height="23" Click="Cancel_Click" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="-0.136"/>
                    <RotateTransform/>
                    <TranslateTransform Y="-0.248"/>
                </TransformGroup>
            </Button.RenderTransform>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <StackPanel Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" x:Name="panel">
                                    <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Width}" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="true">
                                        <Setter Property="Background" Value="Gray" TargetName="panel" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Background" Value="LightGray" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <TextBox x:Name="MaxWeighBox" FontFamily="Fonts/#Cotton Butter"  FontSize="18" HorizontalAlignment="Left" Margin="659,91,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="143"  BorderThickness="0" Padding="0" Background="Transparent" IsReadOnlyCaretVisible="True" />
        <Label x:Name="MaxWeightLable" FontFamily="Fonts/#Cotton Butter" FontSize="18" Content="MaxWeight:" HorizontalAlignment="Left" Margin="558,86,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="BatteryBox" FontFamily="Fonts/#Cotton Butter" FontSize="18" HorizontalAlignment="Left" Margin="663,144,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="142"  BorderThickness="0" Padding="0" Background="Transparent" TextChanged="textBox_Copy_TextChanged" IsReadOnlyCaretVisible="True" />
        <Label x:Name="BatteryLable" FontFamily="Fonts/#Cotton Butter" FontSize="18" Content="Battery:" HorizontalAlignment="Left" Margin="552,138,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="IdBox" FontFamily="Fonts/#Cotton Butter" FontSize="18" HorizontalAlignment="Left" Margin="379,144,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="142"  BorderThickness="0" Padding="0" Background="Transparent" IsReadOnly="True"/>
        <Label x:Name="IdLable" FontFamily="Fonts/#Cotton Butter" FontSize="18" Content="ID:" HorizontalAlignment="Left" Margin="283,141,0,0" VerticalAlignment="Top" />
        <TextBox x:Name="StatusBox" FontFamily="Fonts/#Cotton Butter" FontSize="18" HorizontalAlignment="Left" Margin="666,203,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="143"  BorderThickness="0" Padding="0" Background="Transparent" IsReadOnly="True" IsReadOnlyCaretVisible="True" />
        <Label x:Name="StatusLable" FontFamily="Fonts/#Cotton Butter" FontSize="18" Content="Status:" HorizontalAlignment="Left" Margin="558,198,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="ModelBox" FontFamily="Fonts/#Cotton Butter" FontSize="18" HorizontalAlignment="Left" Margin="378,206,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="142"  BorderThickness="0" Padding="0" Background="Transparent" />
        <Label x:Name="ModelLable" FontFamily="Fonts/#Cotton Butter" FontSize="18" Content="Model:" HorizontalAlignment="Left" Margin="284,201,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="LatBox" FontFamily="Fonts/#Cotton Butter" FontSize="18" HorizontalAlignment="Left" Margin="576,261,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="143"  BorderThickness="0" Padding="0" Background="Transparent" IsReadOnlyCaretVisible="True" />
        <Label x:Name="Lat" FontFamily="Fonts/#Cotton Butter" FontSize="18" Content="Lat:" HorizontalAlignment="Left" Margin="541,256,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="LngBox" FontFamily="Fonts/#Cotton Butter" FontSize="18" HorizontalAlignment="Left" Margin="701,261,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="143"  BorderThickness="0" Padding="0" Background="Transparent" IsReadOnlyCaretVisible="True" />
        <Label x:Name="Lng" FontFamily="Fonts/#Cotton Butter" FontSize="18" Content="Lng:" HorizontalAlignment="Left" Margin="659,256,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="textBox_Copy6" HorizontalAlignment="Left" Margin="67,155,0,0" Text="TextBox" TextWrapping="Wrap" VerticalAlignment="Top" Width="56"  BorderThickness="0" Padding="0" Background="Transparent" IsReadOnlyCaretVisible="True" />
        <Label x:Name="Parcel" HorizontalAlignment="Left" Margin="43,105,0,0" Content="Pacrel"  VerticalAlignment="Top" Width="118"  BorderThickness="0" Padding="0" Background="Transparent" />



    </Grid>
</Window>
